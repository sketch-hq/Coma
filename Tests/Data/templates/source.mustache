

// ----------------------------------------
// Template for the {{name}}.m
// ----------------------------------------

#import "{{name}}.h"

@implementation {{class}}

{{# properties}}
{{getter}}
{{setter}}
{{/ properties}}

// Main template for the encode method

- (void)encodeWithCoder:(NSCoder *)coder {
	{{# properties}}
	// property {{name}} encoding here
	{{/ properties}}
}

- (id)initWithCoder:(NSCoder *)coder {
    self = [super init];
    if (self) {
		{{# properties}}
		// property {{name}} decoding here
		{{/ properties}}
	}
    return self;
}

- (id)copyWithZone:(NSZone *)zone {
        id copy = [self new];
		{{# properties}}
		// property {{name}} copying here
		{{/ properties}}

        return copy;
    }
}

@end
